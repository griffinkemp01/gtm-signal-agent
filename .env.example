# Core
DATABASE_URL=postgresql+psycopg://signal:signal@localhost:5433/signal_agent
LOG_LEVEL=INFO
ENV=dev
# Comma-separated ingestor source names to skip. `competitive` queries Reddit,
# which rate-limits (429) hard on unauthenticated search across many accounts.
DISABLED_INGESTORS=

# Anthropic (LLM validation + scoring summaries)
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-5-20250929

# HubSpot — private app token with scopes:
#   crm.objects.companies.read, crm.objects.companies.write
#   crm.schemas.companies.read, crm.schemas.companies.write
#   timeline (for custom timeline events)
HUBSPOT_ACCESS_TOKEN=
# Portal (hub) id — used to build working "Open in HubSpot" record links.
# Find it at GET https://api.hubapi.com/account-info/v3/details (portalId).
HUBSPOT_PORTAL_ID=
HUBSPOT_TIMELINE_APP_ID=
HUBSPOT_TIMELINE_EVENT_TEMPLATE_ID=

# Slack
SLACK_BOT_TOKEN=
SLACK_SIGNING_SECRET=
SLACK_ALERT_CHANNEL=#gtm-signals-test
SLACK_OWNER_USER_ID=          # who gets pinged when circuit breaker trips

# Clay — outbound last-mile webhook. Qualified accounts are POSTed here; Clay
# does contact discovery + email verification + HubSpot Sequence enrollment.
# Get the URL from a Clay table's "Webhook" import source. Leave blank to
# disable the push. See docs/clay-last-mile.md.
CLAY_WEBHOOK_URL=
# Outbound is decoupled from Slack alerting on a wider, lower bar: an account
# pushes to Clay when its score clears this, even if it never alerts in Slack.
# Lower = more accounts to outbound. Keep well under ALERT_CUMULATIVE_THRESHOLD.
CLAY_PUSH_SCORE_THRESHOLD=6
# Don't re-push (re-enroll) the same account for this many days.
CLAY_PUSH_COOLDOWN_DAYS=30

# Inngest — leave event/signing keys blank for local dev (use `inngest-cli dev`)
INNGEST_APP_ID=signal-agent
INNGEST_EVENT_KEY=
INNGEST_SIGNING_KEY=
INNGEST_DEV=1

# Scoring tuning
# Count at most N signals of the same signal_type toward a company's cumulative
# score, so one news event (which can spawn 40+ articles) can't dominate.
SCORE_MAX_SIGNALS_PER_TYPE=3
ALERT_SCORE_THRESHOLD=8
ALERT_CUMULATIVE_THRESHOLD=12
ALERT_CUMULATIVE_WINDOW_DAYS=60
LLM_CONFIDENCE_FLOOR=0.7
CIRCUIT_BREAKER_ALERTS_PER_HOUR=20
DIGEST_RATE_THRESHOLD=5
DIGEST_FLUSH_INTERVAL_MINUTES=15
# Same-signal-type rolling cooldown (days): a company can't re-alert on
# the same signal_type for this many days after the prior post. Orthogonal
# to the 24h general cooldown.
ALERT_SAME_TYPE_COOLDOWN_DAYS=7
# LLM cache TTL (days). Must be >= ALERT_CUMULATIVE_WINDOW_DAYS so signals
# inside the scoring window never miss cache. Default = 60 + 7 headroom.
LLM_CACHE_TTL_DAYS=67

# Arthur tracing — export LLM calls + pipeline spans to your Arthur Engine.
# ARTHUR_TASK_ID is shown in the Arthur UI at Model Management → API Key after
# you create an Agentic Model.
ARTHUR_TRACING_ENABLED=1
ARTHUR_ENGINE_BASE_URL=http://localhost:3030
ARTHUR_ENGINE_API_KEY=
ARTHUR_TASK_ID=
ARTHUR_SERVICE_NAME=signal-agent
